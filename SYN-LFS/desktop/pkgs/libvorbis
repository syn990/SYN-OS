# Ogg Vorbis, for Audacity's import and export
v=1.3.7
src=("https://downloads.xiph.org/releases/vorbis/libvorbis-$v.tar.xz md5:50902641d358135f06a8392e61c9ac77")
build() {
	./configure --prefix=/usr --disable-static
	make
	make install
}
